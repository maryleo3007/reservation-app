import React from 'react';

const Cash = (props) => {
    
    const {title} = props.cash ;
    
    return (
        <div className="">
            {title}
        </div>
    );
}

export default Cash